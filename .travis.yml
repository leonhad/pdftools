language: cpp

sudo: required

addons:
  apt:
    packages:
      - aclocal-1.5

compiler:
  - clang
  - gcc
dist: trusty

os:
  - linux
  - osx

branches:
  only:
  - master

notifications:
  email: true

