language: cpp

sudo: required

addons:
  apt:
    packages:
      - autotools-dev
      - automake-1.5
      - cppunit

compiler:
  - clang
  - gcc
dist: trusty

os:
  - linux
  - osx

branches:
  only:
  - master

notifications:
  email: true

